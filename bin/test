#!/bin/bash
SUDO="$1"
clear
set -e
if [[ "$SUDO" == "sudo" && "$OSTYPE" == "linux-gnu" ]]; then
	export GO_SPFC_SUDO_TEST=true
fi
go test -covermode=count -coverprofile=coverage.out -v
go tool cover -func=coverage.out
